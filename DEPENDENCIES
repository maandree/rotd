RUNTIME DEPENDENCIES:

	python3
	texlive-bin
	curl (optional: for leap second announcements)
	fortune (optional: for fortune cookies)
	gnupg (optional: for gnupg key expiry warnings)
	solar-python (optional: for solar information)


BUILD DEPENDENCIES:

	make
	coreutils
	sed


INSTALL DEPENDENCIES:

	make
	coreutils

